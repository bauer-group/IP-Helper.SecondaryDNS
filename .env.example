# =============================================================================
# Secondary DNS Server - Konfiguration
# =============================================================================
#
# Kopieren Sie diese Datei nach .env und passen Sie die Werte an:
#   cp .env.example .env
#
# =============================================================================

# -----------------------------------------------------------------------------
# PFLICHTANGABEN
# -----------------------------------------------------------------------------

# IP-Adresse(n) des Primary DNS Servers (Plesk/BIND)
# Mehrere IPs mit Komma trennen: "203.0.113.10,203.0.113.11"
PRIMARY_DNS_IP="203.0.113.10"

# Hostname des Primary DNS Servers
PRIMARY_DNS_HOSTNAME="ns1.example.com"

# E-Mail für System-Benachrichtigungen (Updates, Fehler)
ADMIN_EMAIL="admin@example.com"

# -----------------------------------------------------------------------------
# OPTIONAL - SSH KONFIGURATION
# -----------------------------------------------------------------------------

# SSH Public Key für passwortlosen Login (leer lassen wenn nicht benötigt)
SSH_PUBKEY=""

# Passwort-Login erlauben (true/false)
# Default: true
SSH_PASSWORD_AUTH="true"

# -----------------------------------------------------------------------------
# OPTIONAL - SYSTEM KONFIGURATION
# -----------------------------------------------------------------------------

# Hostname für diesen Server (ohne Domain)
# Default: ns2
SECONDARY_HOSTNAME="ns2"

# FQDN für diesen Server
# Default: ns2.local
SECONDARY_FQDN="ns2.example.com"

# Zeitzone
# Default: Etc/UTC
TIMEZONE="Etc/UTC"

# -----------------------------------------------------------------------------
# OPTIONAL - AUTO-UPDATE KONFIGURATION
# -----------------------------------------------------------------------------

# Automatischer Reboot bei Kernel-Updates (true/false)
# Default: true
AUTO_REBOOT="true"

# Uhrzeit für automatische Reboots (HH:MM)
# Default: 03:00
REBOOT_TIME="03:00"

# -----------------------------------------------------------------------------
# OPTIONAL - TSIG-KEY FÜR SICHERE ZONE-TRANSFERS
# -----------------------------------------------------------------------------

# TSIG-Key für authentifizierte Zone-Transfers (optional, erhöht Sicherheit)
# Format: name:algorithm:secret
# Beispiel: "transfer-key:hmac-sha256:BASE64ENCODEDKEY=="
# Der gleiche Key muss auf dem Primary DNS konfiguriert sein!
# Leer lassen wenn nicht benötigt (IP-Whitelist reicht für die meisten Fälle)
TSIG_KEY=""
